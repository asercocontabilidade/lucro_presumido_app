services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: victorG-lucro-presumido-app
    restart: unless-stopped
    ports:
      - "3003:3003"
      - "3006:3003"
    environment:
      - NODE_ENV=production
      - PORT=3003
    env_file:
      - backend/.env
